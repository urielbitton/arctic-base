import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({ 
  container: {  
    flex: 1
  },
  homeMoviesRow: {
    marginTop: 40,
    marginBottom: 30,
    width: '100%',
    paddingBottom: 20
  }
})